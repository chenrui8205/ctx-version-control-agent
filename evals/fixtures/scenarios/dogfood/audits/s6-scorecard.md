# S6 scorecard (Riley wrap-up)
- multi-close mechanics: both lifecycle closes clean (conf .97/.98), no false conflicts; constraint kept
  complementary ✓ ; chipin-auth new ✓ ; final open set EXACTLY ground truth: {demo-seed-data, chipin-auth} ✓
- DF-1 RECURRENCE x2 (predicted): state_change + close double-superseded deploy step (deploy record lost;
  fly.dev URL survived only via close body); decision + close double-superseded receipt question
  (RECEIPTS DECISION LOST — 2/2 decisions pushed alongside closes this week were eaten)
- action accuracy 4/6 vs ideal GT ; recall 6/6 ; subject reuse 2/2 ; origin correct
- ORGANIC OBSERVABILITY EVIDENCE: Riley noticed Alex's step missing from open-threads, shrugged, moved on
  ("Saturday") — exactly how DF-2 damage presents in real teams: silently absorbed
- UX: agent itself suggested cross-type supersede fixture pairs (independent confirmation); skill should
  state decision+close both-push convention; want `threads --closed-since <commit>` diff view
- master≡HEAD ✓ ; 6 commits, 20 master rows
