# Copy to .env (read by pydantic-settings; env vars override)
CTXVCS_DATABASE_URL=postgresql+psycopg://ctxvcs:ctxvcs@localhost:5433/ctxvcs
# CTXVCS_RECONCILE_MODEL=claude-sonnet-5
# CTXVCS_EMBED_PROVIDER=openai          # or: fake (tests/dev without a key)
# ANTHROPIC_API_KEY=sk-ant-...          # reconcile classifier (write path)
# OPENAI_API_KEY=sk-...                 # embeddings (write path)
# EVAL_LIVE=1                           # required for live eval runs (§12.1)
